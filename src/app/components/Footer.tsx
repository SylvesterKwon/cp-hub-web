import { Typography } from "@mui/material";

export default function Footer({}) {
  return (
    <footer>
      <Typography variant="body2" align="center" color="grey">
        CP-Hub by SylvesterKwon
      </Typography>
    </footer>
  );
}
